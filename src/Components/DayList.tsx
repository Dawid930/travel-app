import { useState } from "react";
import { StandardButton } from "./Style"



/* const DayList = () => {
    const [list, setList] = useState([dayNumber: "", dayDesc: "" ]);
  return (
    <li key={list.dayNumber}>  
    <h1>
      {list.dayNumber && "Day: "}
      {list.item.dayNumber}
    </h1>
    <p>{list.dayDesc}</p>
    {list.dayNumber && <StandardButton>Modify</StandardButton>}
  </li>
  )
} */

//export default DayList